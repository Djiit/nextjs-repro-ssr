{
  "version": 2,
  "builds": [{ "src": "next.config.js", "use": "@now/next" }],
  "routes": [{ "src": "/app(.*)", "dest": "$1" }],
  "build": {
    "env": {
      "BUILDING_FOR_NOW": "true"
    }
  }
}